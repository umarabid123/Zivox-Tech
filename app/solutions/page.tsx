import type { Metadata } from "next";
import SolutionsSection from "@/components/sections/Solutions";

export const metadata: Metadata = { title: "Solutions — Nexvora Tech" };

export default function Page() {
  return <SolutionsSection />;
}
