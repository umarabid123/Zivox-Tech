import type { Metadata } from "next";
import BlogRagSupportSection from "@/components/sections/BlogRagSupport";

export const metadata: Metadata = { title: "Why most RAG chatbots fail — Zivox" };

export default function Page() {
  return <BlogRagSupportSection />;
}
